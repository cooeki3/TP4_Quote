/** @type {import('next').NextConfig} */
const nextConfig = {};
//Uncomment and remove the above line to disable Next's debug tools
// const nextConfig = { devIndicators: false };

export default nextConfig;
